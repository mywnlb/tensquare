server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/libown/tensquareconfig.git
  rabbitmq:
    host: 192.168.85.128
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh