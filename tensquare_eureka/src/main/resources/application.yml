server:
  port: 8888
spring:
  application:
    name: tensquare-eureka
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://192.168.85.128:${server.port}/eureka